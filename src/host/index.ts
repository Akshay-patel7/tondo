// oxlint-disable unicorn/no-empty-file -- Stage 2 gives this entry its code.

// The host runs in an Electron utility process and will own pi, git and the
// terminals. The main process starts it in Stage 2. Until then this entry only
// proves that the host builds as its own CommonJS bundle.
